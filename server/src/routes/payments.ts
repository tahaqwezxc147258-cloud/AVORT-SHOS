import { Router } from 'express'; import { supabase } from '../db.js'; import { requireUser } from '../middleware/auth.js'; const router=Router(); router.post('/create',requireUser,async(req,res)=>{const {data,error}=await supabase.from('Order').select('*').eq('id',String(req.body.orderId)).eq('userId',(req as any).user.sub).single();if(error)return res.status(404).json({error:'Order not found'});res.json({mode:'test',orderId:data.id,amount:data.totalAmountToman});}); router.get('/callback',(_req,res)=>res.status(400).send('Payment callback requires server verification')); export default router;
