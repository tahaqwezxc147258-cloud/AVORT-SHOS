import React from 'react';
import { seoArticles } from '../data/seoArticles';
const pages:Record<string,{title:string;body:string}>={about:{title:'درباره ما',body:'Avort فروشگاه مستقل آنلاین انتخاب کفش است.'},contact:{title:'تماس با ما',body:'تماس: 09382475438'},'purchase-rules':{title:'قوانین خرید',body:'پیش از خرید مشخصات و شرایط ارسال را مطالعه کنید.'},returns:{title:'شرایط مرجوعی',body:'کالا باید استفاده‌نشده و همراه بسته‌بندی باشد.'},shipping:{title:'شرایط ارسال',body:'زمان تحویل به مقصد و شرکت حمل‌ونقل بستگی دارد.'},privacy:{title:'حریم خصوصی',body:'اطلاعات کاربران فقط برای پردازش سفارش استفاده می‌شود.'}};
export const SeoPages:React.FC<{path:string}>=({path})=>{const slug=path.replace(/^\//,'');const article=slug.startsWith('articles/')?seoArticles.find(a=>a.slug===slug.slice(9)):null;const page=pages[slug];return <main className="max-w-4xl mx-auto px-4 py-12 min-h-[60vh]" dir="rtl">{article?<><p className="text-cyan-600 font-bold mb-3">مجله Avort</p><h1 className="text-3xl font-black text-slate-900">{article.title}</h1><img src="/favicon.svg" alt={`تصویر ${article.title}`} className="mt-6 h-32 w-32 rounded-3xl"/><p className="mt-5 text-lg leading-9 text-slate-600">{article.intro}</p>{article.sections.map(s=><section key={s.heading} className="mt-8"><h2 className="text-xl font-black">{s.heading}</h2><p className="mt-3 leading-9 text-slate-600">{s.body}</p></section>)}<nav className="mt-10 border-t pt-6"><h2 className="font-black">مطالب مرتبط</h2>{seoArticles.slice(0,4).map(a=><a className="block text-cyan-700 mt-2" href={`/articles/${a.slug}`} key={a.slug}>{a.title}</a>)}</nav></>:<><h1 className="text-3xl font-black">{page?.title||'مقالات راهنمای خرید کفش'}</h1><p className="mt-6 leading-9 text-slate-600">{page?.body||'راهنماهای انتخاب و نگهداری کفش را بخوانید.'}</p>{!page&&seoArticles.map(a=><a className="block text-cyan-700 mt-3" href={`/articles/${a.slug}`} key={a.slug}>{a.title}</a>)}</>}</main>};
export { pages };
